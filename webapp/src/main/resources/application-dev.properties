
stripe.secret=xxx
stripe.webhook-secret-ps1=xxx
stripe.webhook-secret-ps2=xxx

quarkus.oidc.token.forced-jwk-refresh-interval=1M
quarkus.oidc.jwks.cache-size=10
# Optional: refresh if verification fails
quarkus.oidc.jwks.resolve-early=true

# verbose logging
quarkus.http.access-log.enabled=true
quarkus.log.category."io.quarkus.oidc".level=DEBUG


